{
    "name": "server",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@vapi-ai/server-sdk": "^0.2.2",
        "zod": "^3.24.1",
        "schema": "workspace:*"
    },
    "devDependencies": {
        "tsx": "^4.19.2"
    }
}
